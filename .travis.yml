sudo: required
services:
  - docker

script:
  - docker build -t peterson1873/trade-logger .

deploy:
  provider: elasticbeanstalk
  region: "us-west-2"
  app: "trade-logger"
  env: "TradeLogger-env"
  bucket_name: "elasticbeanstalk-us-west-2-866521079721"
  bucket_path: "trade-logger"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: "$AWS_SECRET_KEY"
